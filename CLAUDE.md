# CLAUDE.md — risk_sim ワークフロー運用ルール

このファイルはプロジェクトルートに置き、Claude Code起動時に必ず読ませる。
ここに書かれたルールは、個別の指示より優先する。

---

## このプロジェクトの目的

シニア向け人生リスク系チャンネル（失敗シミュレーション＋解説構成）の動画制作を、
台本自動生成パイプラインで支援する。v5.1相当の設計思想を引き継ぎ、
risk_simジャンル対応・全文一括執筆方式に作り替えたもの。

対象読者：Claude Code / Claude (Sonnet, Opus, Fable) / Codex。
このファイルを読むエージェントは、まず本ルールに従うこと。

---

## モデル使用ルール（最重要・厳守）

| 工程 | モデル | 理由 |
|---|---|---|
| 企画3案生成（B-1） | Claude Code / Sonnet 4.6 | 仕様が明確な発散作業。上位モデル不要 |
| 台本執筆（B-2） | Claude Code / Opus 4.8 | 品質の核。唯一投資すべき工程 |
| 品質批評・監査（B-4） | Fable 5（枠に余裕がある時）／Opus 4.8（通常） | 判断の質が結果を左右する工程 |
| メタデータ整形（B-6） | Haiku 4.5 または ChatGPT | 定型変換のみ |
| 指示文・プロンプト・JSON叩き台作成全般 | Sonnet 4.6 | 仕様展開作業であり上位モデル不要 |
| ジャンルJSON設計の最終レビューのみ | Opus 4.8（1回だけ） | ゾーン配分・結末パターンは上流品質を規定するため |
| Pythonコードの新規実装・保守・改修 | Claude Code / Sonnet 4.6（デフォルト）。Claude枠が逼迫した場合のみCodexに切り出す | 通常はClaude枠内で完結させる。逼迫時のみCodexへ退避 |
| ブレスト・コピー案・リサーチ深掘り・画像プロンプト改良 | ChatGPT（Claude枠を使わない） | 失敗コストが低い発散作業 |
| 月次バッチ収集 | Antigravity（Gemini） | 無料枠、動画制作フローから独立 |

判断に迷ったら基準は一つ：**「この作業の失敗は高くつくか」**。
高くつく（台本全体・10本に波及する）→ 上位モデルで確認。
やり直しが安い（指示文・下書き）→ Sonnetで十分。

---

## セッション運用ルール（厳守）

1. **同一セッション内で `/model` を切り替えない。** 履歴の再読み込みで高コストになる。
   モデルを変える場合は新規セッションを開始する。
2. **工程の区切りでセッションを終了する。** 受け渡しは会話履歴ではなくファイルで行う。
3. **Claude Codeに渡すのは前工程の成果物ファイルのみ。** 過去の会話履歴に依存させない。
   （例：B-2はblueprint.jsonだけを読み込んで執筆する。それ以前の企画検討過程は渡さない）
4. 縮退運転（Claude枠が逼迫している週）：B-2をSonnet 4.6に落とし、
   B-4の批評をOpusで厚めに行う（執筆を落として監査で拾う）。

### コミット・push確認の運用（001の制作経験を踏まえて更新）

以下は都度の確認を省略し、Claude Codeの判断でコミットまで進めてよい：
- ADRの新規作成・追記
- `videos/{video_id}/` 配下の個別ファイル（`blueprint.json`、
  `FINAL_SCRIPT_FULL.md`等）の、指示内容通りの機械的な更新

以下は引き続き、コミット前に必ず内容を確認する：
- `genres/risk_sim.json`、`prompts/write_full_script.md`、
  `src/production_extractor.py`（ジャンル全体・全動画に波及するため）

pushは、作業の区切り（1つの工程が完了した時点等）でまとめて行ってよい。
毎回immediate pushする必要はない。

---

## ファイル正本方式（Single Source of Truth）

以下のファイルが各工程の正本。これ以外（チャット履歴、記憶）を正としない。

| ファイル | 生成工程 | 内容 |
|---|---|---|
| `videos/{video_id}/blueprint.json` | B-1 | 選定された企画案（テーマ・プロット・結末パターン・サムネ方向） |
| `videos/{video_id}/FINAL_SCRIPT_FULL.md` | B-2 | 全文一括執筆された台本 |
| `videos/{video_id}/quality_report.md` | B-4 | 6軸ルーブリック品質批評と修正指示 |
| `videos/{video_id}/output/voice_plain_text.txt` | B-5 | Vrew用ナレーション素材 |
| `videos/{video_id}/output/voice_assignment.csv` | B-5 | 話者割当 |
| `videos/{video_id}/output/image_prompts_final.json` | B-5 | 画像生成プロンプト |
| `videos/{video_id}/metadata_final.json` | B-6 | 確定したタイトル3案・概要欄・タグ。output/metadata_draft.jsonは実行のたびに再生成される叩き台であり別物 |
| `genres/risk_sim.json` | Phase 0-3 | ジャンル設計（ゾーン構造・結末パターン・chars_per_scene） |

各工程の担当エージェントは、この表にあるファイルの読み書きだけで完結させること。

### B-6運用上の注記

B-6完了後は、必ずmetadata_final.jsonとして動画ルート直下に確定版を保存すること。output/配下のファイルはproduction_extractor.py実行のたびに再生成されるため、Git管理下に置いても意味がない。

### 動画ごとの成果物の置き場所

動画ごとの成果物（`blueprint.json`、`FINAL_SCRIPT_FULL.md`、`quality_report.md` 等）は
`videos/{video_id}/` 以下に置く。`video_id` の命名規則は
**「3桁連番_テーマの短いスラッグ」**（例：`001_taishokukin_shousagi`）とする。

企画案のフォーマットは `videos/_template/blueprint_template.json` を参照する。

### 制作素材（画像・音声・字幕・完成動画）の保存先について

シーン画像・音声ファイル・字幕ファイル・完成動画は、リポジトリの外部
（例：`D:\risk_sim_media\{video_id}\` 等）に保存する。理由：

- 数百MB〜数GB単位の巨大なバイナリで、Git管理に向かない
- これらは制作の「素材・完成品」であり、パイプラインの設計・進捗を記録する
  成果物（`blueprint.json`、`FINAL_SCRIPT_FULL.md`、ADR等）とは性質が異なる

外部保存先のフォルダ命名は `video_id` に対応させ、どの動画の素材かが
フォルダ名から分かるようにする。

---

## 編集禁止・注意事項

- `.py` ファイルはClaude Code（Sonnet 4.6）が直接実装する。Codexに切り出すのは
  Claude枠が逼迫した場合の退避先であり、通常運用ではない。
- `genres/risk_sim.json` を確定する前は、必ずOpus 4.8で1回レビューを通す。
- Part分割・MISSION方式は廃止済み。全文一括執筆プロンプトのみを使う。

---

## ADR（意思決定記録）

`docs/decisions/` 以下に1決定1ファイルで記録する。商材（制作プロセス解説）の素材になるため必須。

テンプレート：
```
# ADR-XXX: [決定タイトル]

## 背景
（何が問題だったか）

## 決定
（何を決めたか）

## 捨てた選択肢と理由
（他に検討した案と、採用しなかった理由）

## 結果
（実施後、どうなったか。後日追記）
```

---

## 検証フェーズの初期方針（最初の10本）

- 尺：検証速度優先なら12〜15分、通常20〜24分（本数投入前に固定する）
- テーマ約5種 × フック2型で配分
- 評価指標：CTR、冒頭30秒維持率、平均視聴維持率（特に解説パート突入時の離脱率）
- 解説パートの離脱率が高い場合：後置きの塊 → 物語内分散 or 圧縮をテストする

### 結末パターンの扱い方（重要）

結末パターン（complete_failure / near_miss / small_lesson / recovery）は
**risk_sim.json側に比率として実装しない。** パイプラインに比率を強制させると、
根拠のない初期仮説が「守るべき設計」として固定化されてしまう。

- 結末パターンは動画ごとに `blueprint.json` の `ending_pattern` フィールドで指定する（人間判断＝B-1）
- risk_sim.json側はenumの選択肢と各パターンの定義のみを持つ

**最初の10本の配分ガイドライン**（強制ではなく目安）：
完全失敗4 ／ 寸前回避3 ／ 小失敗学習3 ／ 感動回復0

理由：
- 立ち上げ期の再生はほぼブラウズ・関連経由の一見視聴であり、「常連の疲労」を考慮する必要がまだない
- 完全失敗型はサムネ・タイトルのインパクトとコメント誘発力が最も強く、CTR・エンゲージメントが生命線の初期段階で優先すべき武器
- 感動回復型は既存フォーマットが飽和しており、失敗シミュレーション仮説の検証にはノイズになる。1本＝10%では判断材料にもならないため初期10本からは除外し、チャンネルが回り始めてから緩急のカードとして戻す
- 近いテーマを異なる結末パターンで対にして投入し、CTR・維持率・コメント率をパターン別に比較できるようにする
- 定常運用の配分（20/40/30/10など）はデータが出てから決定する。それまでは参照値に留める

---

## 人間の判断ポイント（3回のみ）

- B-1：企画3案から1案を選択
- B-4：台本の承認
- B-11：サムネ2〜3案から1案を選択

これ以外の工程はエージェントに委任してよい。
